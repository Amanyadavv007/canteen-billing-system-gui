# canteen-billing-system-gui
This is a simple Java-based console application designed to manage basic canteen billing operations. The system allows users to browse a list of available food items, add selected items to the cart, calculate the total bill, and generate a final receipt. It uses core Java concepts such as classes, objects, ArrayList, and basic input/output handling
